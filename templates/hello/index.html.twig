{% extends "base.html.twig" %}

{% block body %}

	<div>Look at all the messages:
		{% if messages | length > 0  %}

			{% for key,message in messages | slice(0, limit) %}

				<div>
					{# <a href="/messages/{{key}}">{{message.message}}</a> #}
					<a href='{{path ('app_show_one', {id: key}) }}'>{{message.message}}</a>

					<div>
						{{include('hello/_message.html.twig', )}}
					{% endfor %}
				{% else %}
					There's nothing here yet!Okokio
				{% endif %}
				<div>
				{% endblock %}
				{% block title %}
					I changed the Title!
				{% endblock %}
